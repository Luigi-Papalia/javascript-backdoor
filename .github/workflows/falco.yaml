name: Falco Detection

on:
    workflow_dispatch:

jobs:
  falco-security-detection:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
    - name: Start Falco
      uses: falcosecurity/falco-actions/start@v0.39
      with:
        mode: analyze
        
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build Docker image
      run: docker build -t ghcr.io/${{ github.repository_owner }}/vulnerable-app:latest .

    - name: Push Docker image to GHCR
      run: docker push ghcr.io/${{ github.repository_owner }}/vulnerable-app:latest    
   

    - name: Stop Falco
      uses: falcosecurity/falco-actions/stop@v0.39
      with:
        mode: analyze
  
  analyze-falco-security:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
    - name: Analyze
      uses: falcosecurity/falco-actions/analyze@v0.39
      with:
        falco-version: '0.39.0'
